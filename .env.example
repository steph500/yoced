# Optional. Without both of these the contact form composes a pre-filled
# mailto: link instead of sending directly — it never reports a false send.
RESEND_API_KEY=
INQUIRY_FROM="YOCED <inquiries@yoced.com>"
